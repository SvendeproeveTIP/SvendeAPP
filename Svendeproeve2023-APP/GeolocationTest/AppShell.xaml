<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GeolocationTest.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GeolocationTest"
    xmlns:views="clr-namespace:GeolocationTest.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--Loading Page-->
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:LoadingPage}"
        Route="loading" />

    <!--Login Page-->
    <ShellContent 
        Title="Login" 
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="login"/>

    <!--Main Page-->
    <TabBar>
        <Tab Title="Map" Icon="elscooter.png">
            <ShellContent Title="Map" Icon="elscooter.png" ContentTemplate="{DataTemplate views:HomePage}" Route="home" />
        </Tab>
        <Tab Title="Scan" Icon="scan.png">
            <ShellContent Title="Scan" Icon="scan.png" ContentTemplate="{DataTemplate views:Scan}" Route="scanning" />
        </Tab>
        <Tab Title="Orders" Icon="journal_check.svg">
            <ShellContent Title="Orders" Icon="journal_check.svg" ContentTemplate="{DataTemplate views:OrderPage}" Route="orders" />
        </Tab>
        <Tab Title="Settings" Icon="gear_fill.svg">
            <ShellContent Icon="house_door_fill.svg" Title="Settings" ContentTemplate="{DataTemplate views:SettingsPage}" Route="settings" />
        </Tab>
    </TabBar>
</Shell>
